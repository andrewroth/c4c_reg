<h2>Administrator Home</h2>
<h1>All Events:</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Start Date</th>
  </tr>

<% @events.each do |event| %>
  <tr>
    <td><%=h event.name %></td>
    <td>
    <%=h format_event_start_date(event) %>
    </td>
    <td><%= link_to 'Administrate', event %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New event', new_event_path %>
